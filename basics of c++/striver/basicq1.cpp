#include<bits/stdc++.h>///thsi is father of all directory all libraries are take 
using namespace std;
int main(){
    int age;
    cout<<"Enter youer age"<<endl;
    cin >> age;

    if(age >= 18) {
        cout<<"You are adult"<<endl;

    } else {
        cout<<"You are not adult"<<endl;
    }
    return 0;
}